# AGENTS.md

このファイルは、manatubbs リポジトリで作業する AI エージェント向けのガイドです。

## プロジェクト概要

manatubbs は、PHP と SQLite で動作するスレッド型／ツリー型の掲示板です。Composer やフレームワークには依存せず、`index.php` から設定と掲示板エンジンを読み込む構成です。

## 主なファイルとディレクトリ

- `index.php`: エントリーポイント。ローカル設定を読み込みます。
- `setting-org.ini.php`: 配布用の設定例です。
- `setting-user.ini.php`: 実行環境固有の設定です。認証情報などを含むため、編集内容をコミットしないでください。
- `mbbs_engine/index.inc.php`: 画面モードとリクエスト処理を担当するメインコントローラーです。
- `mbbs_engine/inc/`: DB、フォーム、管理画面、共通処理の実装です。
- `mbbs_engine/tpl/`: HTML と RSS のテンプレートです。
- `mbbs_engine/resource/`: CSS などの静的リソースです。
- `tests/`: PHP で直接実行する回帰テストです。
- `db/`、`attach/`: 実行時データを保存します。生成された DB、投稿データ、添付ファイルをコミットしないでください。

## 開発環境の準備

1. `setting-org.ini.php` を `setting-user.ini.php` にコピーします。
2. `setting-user.ini.php` の DB パス、ログイン情報、管理者パスワードなどをローカル環境向けに変更します。
3. `db/` と `attach/` を PHP の実行ユーザーが書き込める状態にします。
4. 必要に応じて、リポジトリのルートで `php -S 127.0.0.1:8000` を実行してブラウザから確認します。

実際のパスワード、メールアドレス、Discord Webhook URL、運用データをサンプル設定やテストへコピーしないでください。

## 変更時の方針

- 既存の手続き型 PHP と `m_` 接頭辞を使う命名に合わせ、目的外のリファクタリングは避けてください。
- インデントや配列記法などは、変更するファイルの周辺コードに合わせてください。
- DB アクセスには既存の `m_db_query()`、`m_db_exec()` などを使い、外部入力を SQL へ直接連結しないでください。
- HTML に出力する外部入力は、出力箇所に応じて `htmlspecialchars()` などでエスケープしてください。
- POST による更新処理では、既存の CSRF トークン検証、読み取り専用モード、認証処理を維持してください。
- ファイルアップロードやリソース配信を変更する場合は、拡張子、サイズ、パスの正規化、保存先ディレクトリの境界を確認してください。
- スキーマ変更は既存 DB に対して再実行できるようにし、新規 DB と既存 DB の両方を考慮してください。
- 表示やフォームを変更するときは、該当するコントローラー、`inc/` の生成処理、`tpl/`、CSS を一緒に確認してください。

## 検証

変更した PHP ファイルは、まず構文チェックしてください。

```sh
php -l path/to/changed-file.php
```

回帰テストは、リポジトリのルートから各ファイルを直接実行します。

```sh
php tests/like_test.php
php tests/duplicate_body_test.php
```

テストを追加する場合は、外部サービスや運用 DB に依存させず、SQLite のインメモリ DB や一時データを使って単独実行できる形を優先してください。画面や投稿処理を変更した場合は、ローカルサーバーでも対象の操作を確認してください。

## 作業完了前の確認

- `git diff` と `git status --short` で変更範囲を確認する。
- ローカル設定、DB、添付ファイル、ログ、認証情報が差分に含まれていないことを確認する。
- 変更した PHP ファイルの構文チェックと、関係する回帰テストを実行する。
- 実行できなかった検証がある場合は、その理由を作業報告に明記する。
